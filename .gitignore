node_modules/
dist/
.DS_Store
*.log

# Never commit Concrete Vision exports -- they contain employee and job data.
# Parsing happens entirely in the browser; nothing needs to live in the repo.
*.csv
*.xlsx
*.xls
!samples/*.sample.csv

# The weekly job cost reports, one workbook per plant. The extension rules above
# already cover them; this belts-and-braces the whole folder so a report saved in
# some other format can't slip through.
weekly job costs/
.render-test/

# Screenshots of the live Concrete Vision UI: they contain real job names,
# plants and schedule data. Reference material only -- never committed.
*.png
*.jpg
*.jpeg
*.gif
*.webp
*.pdf
Screenshot*
